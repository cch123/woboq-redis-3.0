<dec f='redis/deps/hiredis/hiredis.h' l='162' type='int redisFormatCommand(char ** target, const char * format)'/>
<def f='redis/deps/hiredis/hiredis.c' l='925' ll='932' type='int redisFormatCommand(char ** target, const char * format)'/>
<doc f='redis/deps/hiredis/hiredis.c' l='913'>/* Format a command according to the Redis protocol. This function
 * takes a format similar to printf:
 *
 * %s represents a C null terminated string you want to interpolate
 * %b represents a binary safe string
 *
 * When using %b you need to provide both the pointer to the string
 * and the length in bytes as a size_t. Examples:
 *
 * len = redisFormatCommand(target, &quot;GET %s&quot;, mykey);
 * len = redisFormatCommand(target, &quot;SET %s %b&quot;, mykey, myval, myvallen);
 */</doc>
